<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/relative_layout_image"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    >

    <ImageView
        android:id="@+id/image_item_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/list_item_movie_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Feel The Light"
            android:layout_alignLeft="@id/image_item_icon"
            android:layout_alignTop="@id/image_item_icon"
            android:layout_alignRight="@id/image_item_icon"
            android:layout_alignBottom="@id/image_item_icon"
            android:gravity="center"
            android:shadowColor="#eaecee"
            />

</RelativeLayout>